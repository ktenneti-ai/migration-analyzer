# Teradata extraction (future milestone)

Will parse Teradata DDL/SQL/JSON exports into canonical `TeradataTable`
objects (recursive view resolution, base-table discovery — spec sections
9-12). Not implemented in Milestone 1.
